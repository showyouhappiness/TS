#!/usr/bin/env ts-node
console.log('hello world')
console.log(process.argv)


//然后进入使用命令行打开
// 首先使用cd命令切到tsdemo1文件夹，然后ls查看tsdemo1文件夹是否有1.ts，
//如果有使用chmod +x 1.ts给它添加执行权限(windows用户不需要这步，否则报错) 然后使用./1.ts（./必须添加）